# 界面完善 · 0911v2

相较 0911v1，已完成与跨平台账号首页和任务概览的整合。

- 右上角 AiGoodBro 使用原生圆角 A 标识，保留无障碍标签。
- 本机 Agent 统计紧随当前使用概览；账号首页使用统一的列表/卡片切换与首位置顶规则。
- 模型与推理强度的系统菜单使用 ViewThatFits，窄空间改为纵向排列，完整选项摘要可由悬浮提示查看。
- 平台切换栏使用最小高度，允许大字号增加高度。
- 余额主值按字符串精确取整，四舍五入后带约等号；详情保留原始值、来源与快照时间，不经过 Double 丢失大数精度。

完整 macOS 构建、26组纯自检、余额边界 fixture 和 Swift 格式检查通过。统一账号列表/卡片的明暗合成界面已核对；真实桌面菜单点击、键盘焦点和保存回读仍需单独验收。
