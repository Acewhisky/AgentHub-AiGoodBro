# 0910v3 · 9.5.18 (32)

2026-09-10，macOS 本地界面与 Grok 登录修复。相对 0910v2：

- 默认进入 Home，总览现有 Codex 账号与所有已检测到的 CLI 账号；各平台保留独立入口和图标。
- Grok 增加“新增账号并登录”“登录 / 重新登录”和“打开 Grok CLI”。新增账号使用独立目录，通过官方 CLI 与浏览器完成授权；登录结束后重新请求官方接口验证。
- “关联已有配置”明确选择 CLI 配置文件夹，显示隐藏目录，并拒绝把应用包作为账号配置。
- Grok 官方接口的零额外额度不再被误判为登录失败；接口没有返回订阅用量时，显示登录验证与数据缺失提示，不推算百分比。
- 模型和思考强度改用直接选择菜单，减少重复子菜单；卡片及重置数量采用更克制的配色。

官方 Grok 登录和账号接口已实际验证。新增独立账号的浏览器授权仍由用户完成；编译、安装及实际 Home 界面的验证在各自回执中记录。重置卡只供用户手动操作，本轮不执行或测试兑换流程。

本版保留 Next 的现有存储与运行命名空间。aigoodbro 品牌、AgentHub 工作台、可折叠账号、自定义 CLI / API、Telegram / 微信和后续性能工作在安装后规划并继续推进。没有重启现用 Hub。
